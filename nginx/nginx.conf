events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;

    # Configuração de upstream para melhor controle de erros
    upstream prometheus_backend {
        server prometheus:9090 max_fails=3 fail_timeout=30s;
    }
    
    upstream grafana_backend {
        server grafana:4000 max_fails=3 fail_timeout=30s;
    }
    
    upstream api_backend {
        server backend:3001 max_fails=3 fail_timeout=30s;
    }

    # ========================================
    # SERVIDOR PRINCIPAL HTTP - automacao.cmm.am.gov.br
    # ========================================
    
    server {
        listen 80;
        server_name automacao.cmm.am.gov.br 172.18.1.32;
        
        root /usr/share/nginx/html;
        index index.html;
        
        # Log para debug
        access_log /var/log/nginx/access.log main;
        error_log /var/log/nginx/error.log warn;
        
        # Proxy reverso para Grafana
        location /grafana/ {
            proxy_pass http://grafana_backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Configurações adicionais para Grafana
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_cache_bypass $http_upgrade;
            
            # Evitar redirecionamentos infinitos
            proxy_redirect http://grafana:4000/ /grafana/;
            proxy_redirect default;
            
            # Ajustes para evitar problemas
            proxy_buffering off;
            proxy_http_version 1.1;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
            
            # Tratamento de erro
            error_page 502 503 504 = @grafana_error;
        }
        
        # Proxy reverso para Prometheus
        location /prometheus/ {
            proxy_pass http://prometheus_backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Timeouts mais rápidos
            proxy_connect_timeout 5s;
            proxy_send_timeout 10s;
            proxy_read_timeout 10s;
            
            # Tratamento de erro
            error_page 502 503 504 = @prometheus_error;
        }
        
        # Proxy reverso para a API de automação
        location /api/ {
            proxy_pass http://api_backend/api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Configurações adicionais para evitar timeouts
            proxy_connect_timeout 30s;
            proxy_send_timeout 30s;
            proxy_read_timeout 30s;
            
            # Tratamento de erro
            error_page 502 503 504 = @api_error;
        }
        
        # Rota para health check da API
        location /api/health {
            proxy_pass http://api_backend/health;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        # Páginas de erro personalizadas
        location @prometheus_error {
            return 503 'Prometheus temporariamente indisponível. Verifique se o serviço está rodando.';
            add_header Content-Type text/plain;
        }
        
        location @grafana_error {
            return 503 'Grafana temporariamente indisponível. Verifique se o serviço está rodando.';
            add_header Content-Type text/plain;
        }
        
        location @api_error {
            return 503 'API temporariamente indisponível. Verifique se o serviço está rodando.';
            add_header Content-Type text/plain;
        }
        
        # Erro genérico para outros serviços
        location @service_error {
            return 503 'Serviço temporáriamente indisponível. Verifique se o container está rodando.';
            add_header Content-Type text/plain;
        }
        
        # Servir o frontend React - qualquer outra rota vai para o frontend
        location / {
            try_files $uri $uri/ /index.html;
        }
        
        # Health check
        location /health {
            return 200 'Nginx OK';
            add_header Content-Type text/plain;
        }
    }

    # ========================================
    # SERVIDOR PRINCIPAL HTTPS - automacao.cmm.am.gov.br
    # ========================================
    
    server {
        listen 443 ssl;
        server_name automacao.cmm.am.gov.br 172.18.1.32;
        
        # Certificados SSL
        ssl_certificate /etc/ssl/certs/server.crt;
        ssl_certificate_key /etc/ssl/certs/server.key;
        
        # Configurações SSL
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers on;
        
        root /usr/share/nginx/html;
        index index.html;
        
        # Log para debug
        access_log /var/log/nginx/access_ssl.log main;
        error_log /var/log/nginx/error_ssl.log warn;
        
        # Proxy reverso para Grafana
        location /grafana/ {
            proxy_pass http://grafana_backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Configurações adicionais para Grafana
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_cache_bypass $http_upgrade;
            
            # Evitar redirecionamentos infinitos
            proxy_redirect http://grafana:4000/ /grafana/;
            proxy_redirect default;
            
            # Ajustes para evitar problemas
            proxy_buffering off;
            proxy_http_version 1.1;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
            
            # Tratamento de erro
            error_page 502 503 504 = @grafana_error;
        }
        
        # Proxy reverso para Prometheus
        location /prometheus/ {
            proxy_pass http://prometheus_backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Timeouts mais rápidos
            proxy_connect_timeout 5s;
            proxy_send_timeout 10s;
            proxy_read_timeout 10s;
            
            # Tratamento de erro
            error_page 502 503 504 = @prometheus_error;
        }
        
        # Proxy reverso para a API de automação
        location /api/ {
            proxy_pass http://api_backend/api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Configurações adicionais para evitar timeouts
            proxy_connect_timeout 30s;
            proxy_send_timeout 30s;
            proxy_read_timeout 30s;
            
            # Tratamento de erro
            error_page 502 503 504 = @api_error;
        }
        
        # Rota para health check da API
        location /api/health {
            proxy_pass http://api_backend/health;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        # Health check
        location /health {
            return 200 'HTTPS OK';
            add_header Content-Type text/plain;
        }
        
        # Páginas de erro personalizadas
        location @prometheus_error {
            return 503 'Prometheus temporariamente indisponível. Verifique se o serviço está rodando.';
            add_header Content-Type text/plain;
        }
        
        location @grafana_error {
            return 503 'Grafana temporariamente indisponível. Verifique se o serviço está rodando.';
            add_header Content-Type text/plain;
        }
        
        location @api_error {
            return 503 'API temporariamente indisponível. Verifique se o serviço está rodando.';
            add_header Content-Type text/plain;
        }
    }

    # ========================================
    # SERVIDOR FALLBACK PARA HTTP
    # ========================================
    
    server {
        listen 80 default_server;
        server_name _;
        
        # Redirecionar para o domínio principal
        return 301 http://automacao.cmm.am.gov.br$request_uri;
    }

    # ========================================
    # SERVIDORES VIRTUAIS PARA SUBDOMÍNIOS
    # ========================================
    
    # Grafana - grafana.cmm.am.gov.br
    server {
        listen 80;
        listen 443 ssl;
        server_name grafana.cmm.am.gov.br;
        
        # Certificados SSL
        ssl_certificate /etc/ssl/certs/server.crt;
        ssl_certificate_key /etc/ssl/certs/server.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        
        # Proxy direto para Grafana
        location / {
            proxy_pass http://grafana_backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Configurações específicas do Grafana
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_cache_bypass $http_upgrade;
            proxy_buffering off;
            proxy_http_version 1.1;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
        }
    }
    
    # Prometheus - prometheus.cmm.am.gov.br
    server {
        listen 80;
        listen 443 ssl;
        server_name prometheus.cmm.am.gov.br;
        
        ssl_certificate /etc/ssl/certs/server.crt;
        ssl_certificate_key /etc/ssl/certs/server.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        
        location / {
            proxy_pass http://prometheus_backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
    
    # N8N - n8n.cmm.am.gov.br
    server {
        listen 80;
        listen 443 ssl;
        server_name n8n.cmm.am.gov.br;
        
        ssl_certificate /etc/ssl/certs/server.crt;
        ssl_certificate_key /etc/ssl/certs/server.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        
        location / {
            proxy_pass http://n8n:5678/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
    
    # Chatwoot - chatwoot.cmm.am.gov.br
    server {
        listen 80;
        listen 443 ssl;
        server_name chatwoot.cmm.am.gov.br;
        
        ssl_certificate /etc/ssl/certs/server.crt;
        ssl_certificate_key /etc/ssl/certs/server.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        
        location / {
            proxy_pass http://chatwoot:3000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
    
    # WhatsApp Evolution - whatsapp.cmm.am.gov.br
    server {
        listen 80;
        listen 443 ssl;
        server_name whatsapp.cmm.am.gov.br;
        
        ssl_certificate /etc/ssl/certs/server.crt;
        ssl_certificate_key /etc/ssl/certs/server.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        
        location / {
            proxy_pass http://whatsapp:3000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
    
    # RabbitMQ - rabbitmq.cmm.am.gov.br
    server {
        listen 80;
        listen 443 ssl;
        server_name rabbitmq.cmm.am.gov.br;
        
        ssl_certificate /etc/ssl/certs/server.crt;
        ssl_certificate_key /etc/ssl/certs/server.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        
        location / {
            proxy_pass http://rabbitmq:15672/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
    
    # Redis - redis.cmm.am.gov.br
    server {
        listen 80;
        listen 443 ssl;
        server_name redis.cmm.am.gov.br;
        
        ssl_certificate /etc/ssl/certs/server.crt;
        ssl_certificate_key /etc/ssl/certs/server.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        
        location / {
            proxy_pass http://redis:8081/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # ========================================
    # SERVIDORES DE DEBUG E MÉTRICAS
    # ========================================

    # Configuração do stub_status para métricas
    server {
        listen 8080;
        location /stub_status {
            stub_status on;
            access_log off;
            allow 127.0.0.1;
            allow 172.0.0.0/8;
            deny all;
        }
        
        location / {
            return 200 'Nginx is running\n';
        }
    }
    
    # Servidor HTTP adicional para debug (porta 8081)
    server {
        listen 8081;
        server_name _;
        
        root /usr/share/nginx/html;
        index index.html;
        
        # Frontend React - todas as rotas
        location / {
            try_files $uri $uri/ /index.html;
        }
        
        # Status do servidor
        location /status {
            return 200 'Servidor HTTP funcionando na porta 8081\n';
            add_header Content-Type text/plain;
        }
    }
}